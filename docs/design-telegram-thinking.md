# SUPERSEDED — drafts and Thinking are gone

The user rejected draft/Thinking APIs as alpha. The live UX is the
classic blue typing indicator: `sendChatAction(typing)` every 4s
while the last-routed agent is active. See
[design-telegram-chat-send.md](design-telegram-chat-send.md).
